# """
# Модуль: requirements.txt
# Назначение: Заказ-наряд на установку внешних зависимостей экосистемы ADSProfile Manager.
# Зона ответственности: Декларация сторонних библиотек, фиксация их стабильных версий,
#                      обеспечение надежной десктопной и асинхронной Web3-автоматизации,
#                      включая асинповое зондирование прокси-каналов (HTTP/SOCKS4/SOCKS5).
# Интеграция: Устанавливается через pip в изолированное виртуальное окружение (venv)
#              перед запуском утилиты.
# """

# =============================================================================
# 🎨 КАРКАС ИНТЕРФЕЙСА И ВИЗУАЛИЗАЦИЯ (GUI ENGINE)
# =============================================================================
# Кроссплатформенный графический бронепоезд Qt6 для рендеринга MainWindow,
# липкого дока StickyDock и нашего фирменного Neon-табло логов
PySide6>=6.6.0

# =============================================================================
# 🤖 АВТОМАТИЗАЦИЯ И СИНТАКСИЧЕСКИЙ РАЗБОР (WEBDRIVER & PARSING)
# =============================================================================
# Многопоточный бурильщик для захвата отладочных портов AdsPower, обхода
# теневого DOM и прецизионного разблокирования Web3-кошельков
selenium>=4.18.0

# Парсер-стилист для лексического разбора и подсветки синтаксиса
# пользовательских скриптов в реальном времени внутри редактора CodeEditor
pygments>=2.17.2

# =============================================================================
# 🚀 АСИНХРОННЫЕ СЕТЕВЫЕ СЛУЖБЫ (ASYNC NETWORKING)
# =============================================================================
# Скоростная асинхронная ракета для массового опроса статусов сотен профилей,
# асинхронного закрытия браузеров и легкого зондирования GeoIP-сервисов
aiohttp>=3.9.3

# Высокопроизводительный асинхронный мост для проброса трафика через
# SOCKS4, SOCKS5 и HTTP прокси-сервера без блокировки Event Loop
aiohttp-socks>=0.8.4

# =============================================================================
# 🛡️ СИСТЕМНАЯ БЕЗОПАСНОСТЬ И WIN32-ИНТЕГРАЦИЯ (WINDOWS OS ENGINE)
# =============================================================================
# Нативный системный мост к API Windows для безопасного шифрования паролей
# в Credential Manager и ухода от небезопасного плоского хранения ключей в реестре
pywin32>=306; sys_platform == 'win32'

# Кроссплатформенный хирургический инструмент для точечной утилизации
# сиротских процессов chromedriver.exe на уровне операционной системы (Resource Guard)
psutil>=5.9.0